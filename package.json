{
  "name": "evolution-lab",
  "version": "1.0.0",
  "scripts": {
    "build": "mkdir public && html-minifier-terser --collapse-whitespace --remove-comments --minify-css true --minify-js true -o public/index.html index.html"
  },
  "devDependencies": {
    "html-minifier-terser": "^7.2.0"
  }
}
